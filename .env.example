# Copy to .env and fill in. Only needed from Phase 8 (paper trading) onward.
# Broker choice is deferred (candidates: OANDA v20 REST, MetaTrader5).

# --- OANDA v20 (practice/live) ---
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
OANDA_ENVIRONMENT=practice   # practice | live

# --- Runtime ---
FXLAB_ENV=dev
